SELECT EMP.POS_ID,
       EMP.CUR_LEVEL,
       EMP.UNIT_ID,
       EMP.ROLE_ID,
       UNT.SECT_ID,
       SEC.DEPT_ID
  FROM KPDBA.EMPLOYEE EMP, KPDBA.UNIT UNT, KPDBA.SECT SEC
 WHERE     EMP.UNIT_ID = UNT.UNIT_ID
       AND UNT.SECT_ID = SEC.SECT_ID
       AND EMP.EMP_ID = :APP_USER_ID